<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uch_plaktika"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".PasswordAppActivity"
            android:exported="false"/>
        <activity
            android:name=".RabocheePr.MainActivity"
            android:exported="false" />
        <activity
            android:name=".CreatePassword"
            android:exported="false" />
        <activity
            android:name=".GetCodeActivity"
            android:exported="false" />
z
        <activity
            android:name=".Zagruzka.OnBoardActivity"
            android:exported="false" />
        <activity
            android:name=".LauncherActivity"
            android:exported="true">

            <!-- Запускаемое активити -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>